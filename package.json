{
  "name": "network-self-awareness",
  "version": "1.0.0",
  "description": "The Network's Self-Awareness — a local agent that evolves a static website via Ollama",
  "type": "module",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
